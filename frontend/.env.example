# Copy this file to .env and fill in your real values. Never commit .env itself.
# Vite requires the VITE_ prefix for env vars to be exposed to frontend code.

# From your Sentry project settings (create a separate "React" project for the frontend)
VITE_SENTRY_DSN_FRONTEND=
